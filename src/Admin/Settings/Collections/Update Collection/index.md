# Update Collection

Edit the same fields available on [Add Collection](/admin/settings/collections/add-collection/) &mdash; Name, Title, Description, Page Template, and whether category detail URLs are generated.

!!! Note
Changing the Page Template changes every category page the Collection generates, the same way a Datatable's display template controls every entry's detail page.
!!!
