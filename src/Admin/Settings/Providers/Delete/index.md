# Delete

Permanently remove a connection.

!!! Note
Removing a provider setting disconnects anything using it. Check what depends on a connection (Segments imports, MCP tokens, Git hosting, SSO login) before deleting it.
!!!
